## Todo List

# Firstly i used the "'use strict' ;" command 
And then i started calling or saving all my Id's in variables,
Then i added an event listener to my add button so that anytime clicked the button it will run the function (newe();). This function create an li and a button element and gives it their text content which for the li is th value of the input, and the append the li element to the already create ul element and then append the button element to the newly created li.
## Random Quote Generator

# Firstly use the "'use strict';" command
And then call out your id's and save them inside of a variable 
Then create a variable for your random quotes and make use of "Math.random" to make it select the quotes randomly i then add an Event listener to my new quote button so it can run a function which on clicked removes any content of where i want the quote to be and the put a new random quote.
